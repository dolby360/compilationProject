int foo(int i, char j, string k)
{
    int k;
    // variable declarations
    int square(int t) // function declarations
    {
        int temp;
        temp = t* t;
        return temp;
    }
    int total; 
    k=5;
    total = i+square(k);
    return total-10;
}